[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "dbx-config"
version = "0.1.0"
description = "Build databricks.sdk Config objects from an explicit env mapping with type coercion."
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["databricks-sdk"]

